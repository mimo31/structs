example<field>
{
_name = "field of real numbers";

infinite_characteristic;
}

example<commutative_ring>
{
_name = "ring of integers";

infinite_characteristic;
Noetherian;
~Artinian;
}

example<field>
{
_name = "field of two elements"

characteristic_2;
set/two-element;
}

example<group>
{
_name = "permutation group on three elements"

~commutative;
finite;
~simple;
}

example<commutative_group>
{
_name = "Klein four-group"

~cyclic;
finite;
}

example<commutative_group>
{
_name = "trivial group"

trivial;
}

example<commutative_monoid>
{
_name = "a commutative monoid on two elements"
_description = "aa = a, ab = b, ba = b, bb = b"

set/two-element;

}

example<semigroup>
{
_name = "a semigroup on two elements"
_description = "aa = a, ab = a, ba = a, bb = a"

set/two-element;
commutative;
~unital;

}

example<semigroup>
{
_name = "a semigroup on two elements"
_description = "aa = a, ab = b, ba = a, bb = b"

set/two-element;
~commutative;
~unital;
}
